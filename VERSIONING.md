# 버전 규칙 (Versioning)

이 프로젝트는 **Semantic Versioning (MAJOR.MINOR.PATCH)** 을 따르며 `0.1.0`에서 시작한다.
현재 버전은 루트 `VERSION` 파일과 코드의 `APP_VERSION` 상수로 관리한다.

## 규칙 표

| 상황 | 변경 | 예시 |
|-----|-----|-----|
| 첫 버전 | 시작값 | `v0.1.0` |
| 마이너 업데이트 (버그픽스 / 소기능) | PATCH +1 | `v0.1.1`, `v0.1.2` … |
| 주요 업데이트 (기능 / UI 마일스톤) | MINOR +1, PATCH 리셋 | `v0.2.0`, `v0.3.0` … |
| RC 완료 (첫 안정판) | MAJOR 1.0.0 | `v1.0.0` |
| 대규모 업데이트 (호환 깨짐 / 데이터 구조 변경) | MAJOR +1 | `v2.0.0`, `v3.0.0` … |

- **MAJOR**: 호환 깨짐 / 세이브·데이터 구조 변경
- **MINOR**: 기능 추가 / UI 개편
- **PATCH**: 버그 수정

## 버전 표시 & 히스토리 (앱 UI 요구사항)

- 창 제목 + 화면 상단 모서리에 `vX.Y.Z` 상시 표시.
- 설정(또는 정보) 메뉴에 **"업데이트 내역"** 항목 — 버전별 날짜·요약을 최신순으로, 버전당 2~4줄로 표시.
- 코드에서는 `APP_VERSION` 상수 + `VERSION_HISTORY` 리스트(버전, 날짜, 요약)로 관리하고 기능 추가 시마다 갱신한다.
- 이 인앱 히스토리는 git 로그성 `CHANGELOG.md`와 별개로, 앱 안에서 바로 확인 가능해야 한다.

## 업데이트 내역

### v0.4.2 (2026-07-10)
- 공격 시각/청각 피드백 추가: 스킬 발동 시 스윙 이펙트(부채꼴 전방 / 회전 격돌 주위) + 합성 효과음(swing/hit/crush/levelup).
- 적이 근처에 없어도 1/2/3 입력이 눈·귀로 즉시 확인되도록 개선(사용자 피드백 "소리도 공격도 안 나" 대응).
- 몬스터 탐지 범위 20으로 확대 — 전부 플레이어에게 몰려옴. 실제 키 입력 시뮬레이션(ParseInputEvent)+스크린샷으로 발동 검증.

### v0.4.1 (2026-07-10)
- 치명 버그 수정: 카메라가 회전하는 플레이어의 자식이라 화면이 통째로 돌던 문제 → 카메라를 분리(CameraFollow, 위치만 추종 + LookAt).
- 이동 불능 수정: navmesh가 y>0에 베이크돼 경로 방향이 0이 되던 문제 → 평지 무장애물 아레나는 직접 이동으로 전환(플레이어·몬스터 공통).
- 카메라 셰이크를 Position→프러스텀 오프셋(HOffset/VOffset)으로 변경(추종과 충돌 제거).
- 실제 창 렌더 스크린샷으로 가시성 확인 + MovementTest 회귀 추가. headless 테스트 5종 전부 통과.

### v0.4.0 (2026-07-10)
- 최종 검수 지적 5건 수정: 플레이어 사망 처리(R키 재시작), 회전 격돌 이동+다단타(RehitInterval).
- 히트스톱을 벽시계 기반으로 리팩터(다중 처치 async 경합 제거), 히트박스 형상 오버라이드 복원, `_UnhandledInput` 이벤트 기반화.
- Windows 실행 파일(.exe, self-contained 180MB) export + 바탕화면 바로가기 생성.
- 사망 경로 회귀 테스트(PlayerDeathTest) 추가 — headless 테스트 4종 전부 통과.

### v0.3.0 (2026-07-10)
- M3 경험치/레벨업(`PlayerProgression`) + 스탯 포인트 분배(4/5/6/7키) 구현.
- M4 아이템 드랍/장비 보너스(간소화: 그리드 UI 없이 처치 즉시 적용) 구현.
- HUD에 레벨·XP·포인트 표시 추가.
- headless 회귀 테스트 3종(로직 9건 + 진행루프 14건 + 전투 통합 2단계) 전부 통과.

### v0.2.0 (2026-07-10)
- M0~M1 씬 구성(Player/Enemy/Main), 등각 카메라, 런타임 내비게이션 베이크.
- M2 몬스터 AI(추격→공격 상태머신) 구현, 실전투 버그 발견·수정(Area3D 판정 지연).
- HUD(체력·마나 구슬 + 업데이트 내역 패널), 데미지 넘버 연동.
- headless 자동 테스트 2종(순수 로직 9건 + 엔진 통합 전투) 전부 통과.

### v0.1.0 (2026-07-10)
- 프로젝트 초기화 (로컬 + GitHub + SVIL Vault).
- 프롤로그 MVP PRD v0.1 확보 (Outline 위키 → docs/prd).
- 버전 규칙 · 문서 이중 저장 규칙 · 에이전트 규칙 파일 정립.
